import styled from 'styled-components/native';

const Actions = styled.View`
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  padding: 16px;
  height: ${56 + 16 + 16}px;
`;

export default Actions;
